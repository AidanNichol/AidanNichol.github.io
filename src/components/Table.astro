---
export interface Props {
  left?: boolean;
  right?: boolean;
}

const { left, right } = Astro.props as Props;
---

<div class="table">
  <slot />
</div>
<style>
  .table {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 0.5rem;
    margin: 0;
  }
</style>
